pyannote.audio>=3.1.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
# torch는 환경에 맞게 별도 설치 권장:
#   CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
#   GPU: pip install torch (PyPI 기본값)
torch>=2.0.0
