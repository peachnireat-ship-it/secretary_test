## 하네스: 리서치

**목표:** 어떤 주제든 웹·학술·커뮤니티 3각도에서 병렬 조사하고, 교차 검증 후 종합 보고서를 작성한다.

**트리거:** 주제 조사·리서치·심층 분석·동향 파악·보고서 작성 요청 시 `research-orchestrator` 스킬을 사용한다. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-30 | 초기 구성 | 전체 | - |

---

## 하네스: 유튜브 콘텐츠 제작

**목표:** 주제를 받아 트렌드 조사 → 대본 → SEO 최적화 → 썸네일 컨셉 순서로 감독자 에이전트가 팀을 조율하여 완성된 콘텐츠 패키지를 작성한다.

**트리거:** 유튜브 영상 기획·대본·SEO·썸네일·콘텐츠 패키지 요청 시 `youtube-orchestrator` 스킬을 사용한다. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-30 | 초기 구성 | 전체 | - |

---

## 하네스: API 문서 자동 생성

**목표:** secretary_test 서비스 레이어(storage.js, claude.js, groqStt.js)의 export 함수를 분석하여 엔드포인트 추출 → 문서 작성 → 예제 생성 → 완성도 리뷰 파이프라인으로 최종 API 문서 패키지를 자동 생성한다.

**트리거:** API 문서 생성·함수 문서화·서비스 문서 작성·예제 생성·문서 리뷰 요청 시 `api-doc-orchestrator` 스킬을 사용한다. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-30 | 초기 구성 | 전체 | - |

---

## 하네스: 마케팅 캠페인 제작

**목표:** 제품·서비스를 받아 시장 조사 → 광고 카피 → 비주얼 컨셉 → A/B 테스트 계획 → 품질 리뷰 파이프라인으로 최종 캠페인 패키지를 자동 생성한다.

**트리거:** 마케팅 캠페인·광고 카피·비주얼 컨셉·A/B 테스트·캠페인 기획·캠페인 리뷰 요청 시 `campaign-orchestrator` 스킬을 사용한다. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-30 | 초기 구성 | 전체 | - |

---

## 하네스: 데이터 파이프라인 설계

**목표:** 소스·처리 목표·싱크를 받아 아키텍처 초안 → 스키마·ETL 협업 설계 → 검증 규칙 → 모니터링 설정 → 최종 파이프라인 스펙을 계층적 위임으로 자동 생성한다.

**트리거:** 데이터 파이프라인·ETL·스키마 설계·데이터 검증 규칙·파이프라인 모니터링·파이프라인 스펙 요청 시 `pipeline-orchestrator` 스킬을 사용한다. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-30 | 초기 구성 | 전체 | - |

---

## 하네스: 웹툰 에피소드 제작

**목표:** 에피소드 정보를 받아 스토리 작성 → 창작팀(캐릭터·패널·대사) 상호 스타일 일관성 리뷰 → 최종 스타일 검증 → 에피소드 패키지를 하이브리드 에이전트 팀으로 자동 생성한다.

**트리거:** 웹툰 에피소드 제작·스토리 작성·캐릭터 디자인 프롬프트·패널 레이아웃·대사 편집·스타일 일관성 검증·에피소드 패키지 요청 시 `webtoon-orchestrator` 스킬을 사용한다. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-30 | 초기 구성 | 전체 | - |

---

## 하네스: 풀스택 웹사이트 개발

**목표:** 웹사이트 요구사항을 받아 요구사항 분석 → UX 디자인 → 프론트엔드·백엔드 병렬 구현(API contract 선합의) → QA 테스트 → 배포 설정까지 6개 에이전트가 와이어프레임부터 배포까지 완성한다.

**트리거:** 웹사이트 개발·Next.js 프로젝트·풀스택 앱·와이어프레임부터 배포·React/Next.js 프론트엔드·백엔드 API 구현·웹앱 개발 요청 시 `web-dev-orchestrator` 스킬을 사용한다. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-30 | 초기 구성 | 전체 | - |

---

## 하네스: 종합 코드 리뷰

**목표:** 코드베이스를 받아 아키텍처·보안 취약점·성능 병목·코드 스타일을 4개 에이전트가 병렬 감사하고, review-synthesizer가 교차 분석하여 우선순위 정렬된 종합 리포트를 생성한다.

**트리거:** 코드 리뷰·아키텍처 감사·보안 취약점 점검·성능 병목 분석·코드 스타일 감사·리뷰 리포트 요청 시 `code-review-orchestrator` 스킬을 사용한다. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-30 | 초기 구성 | 전체 | - |
